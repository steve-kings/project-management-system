# Architecture Documentation

## System Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                           CLIENT BROWSER                             │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │                    React Application                          │  │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐          │  │
│  │  │   Login     │  │  Dashboard  │  │  Projects   │          │  │
│  │  │    Page     │  │    Page     │  │    Page     │          │  │
│  │  └─────────────┘  └─────────────┘  └─────────────┘          │  │
│  │                                                                │  │
│  │  ┌─────────────────────────────────────────────────────────┐ │  │
│  │  │           Redux Store (State Management)                │ │  │
│  │  │  - authSlice (user authentication)                      │ │  │
│  │  │  - workspaceSlice (workspaces, projects, tasks)        │ │  │
│  │  │  - themeSlice (dark/light mode)                        │ │  │
│  │  └─────────────────────────────────────────────────────────┘ │  │
│  │                                                                │  │
│  │  ┌─────────────────────────────────────────────────────────┐ │  │
│  │  │              API Service Layer (Axios)                  │ │  │
│  │  │  - authAPI (login, logout, getMe)                      │ │  │
│  │  │  - workspaceAPI (CRUD operations)                      │ │  │
│  │  │  - projectAPI (CRUD operations)                        │ │  │
│  │  │  - taskAPI (CRUD operations)                           │ │  │
│  │  └─────────────────────────────────────────────────────────┘ │  │
│  └───────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    │ HTTPS/HTTP
                                    │ REST API Calls
                                    │ (JSON)
                                    ↓
┌─────────────────────────────────────────────────────────────────────┐
│                         EXPRESS.JS SERVER                            │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │                      Middleware Layer                         │  │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐    │  │
│  │  │   CORS   │→ │  Session │→ │ Passport │→ │  Cookie  │    │  │
│  │  │  Config  │  │  Manager │  │   Auth   │  │  Parser  │    │  │
│  │  └──────────┘  └──────────┘  └──────────┘  └──────────┘    │  │
│  └───────────────────────────────────────────────────────────────┘  │
│                                                                      │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │                      API Routes                               │  │
│  │                                                                │  │
│  │  /api/auth/*                                                  │  │
│  │  ├─ GET  /google              → Initiate OAuth               │  │
│  │  ├─ GET  /google/callback     → OAuth callback               │  │
│  │  ├─ GET  /me                  → Get current user (Protected) │  │
│  │  └─ POST /logout              → Logout (Protected)           │  │
│  │                                                                │  │
│  │  /api/workspaces/*                                            │  │
│  │  ├─ GET  /                    → Get all workspaces           │  │
│  │  └─ POST /                    → Create workspace             │  │
│  │                                                                │  │
│  │  /api/projects/*                                              │  │
│  │  ├─ POST   /                  → Create project               │  │
│  │  ├─ PUT    /:id               → Update project               │  │
│  │  └─ DELETE /:id               → Delete project               │  │
│  │                                                                │  │
│  │  /api/tasks/*                                                 │  │
│  │  ├─ POST   /                  → Create task                  │  │
│  │  ├─ PUT    /:id               → Update task                  │  │
│  │  ├─ DELETE /                  → Delete tasks (bulk)          │  │
│  │  └─ POST   /:id/comments      → Add comment                  │  │
│  └───────────────────────────────────────────────────────────────┘  │
│                                                                      │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │                  Authentication Middleware                    │  │
│  │  - Verify JWT token from cookie                              │  │
│  │  - Attach user to request object                             │  │
│  │  - Reject unauthorized requests                              │  │
│  └───────────────────────────────────────────────────────────────┘  │
│                                                                      │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │                    Mongoose Models                            │  │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐    │  │
│  │  │   User   │  │Workspace │  │ Project  │  │   Task   │    │  │
│  │  │  Schema  │  │  Schema  │  │  Schema  │  │  Schema  │    │  │
│  │  └──────────┘  └──────────┘  └──────────┘  └──────────┘    │  │
│  └───────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    │ MongoDB Protocol
                                    │ (Mongoose ODM)
                                    ↓
┌─────────────────────────────────────────────────────────────────────┐
│                         MONGODB ATLAS                                │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │                      Database: project-management             │  │
│  │                                                                │  │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐       │  │
│  │  │    users     │  │  workspaces  │  │   projects   │       │  │
│  │  │  Collection  │  │  Collection  │  │  Collection  │       │  │
│  │  └──────────────┘  └──────────────┘  └──────────────┘       │  │
│  │                                                                │  │
│  │  ┌──────────────┐                                             │  │
│  │  │    tasks     │                                             │  │
│  │  │  Collection  │                                             │  │
│  │  └──────────────┘                                             │  │
│  └───────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────┘

                    ┌──────────────────────────┐
                    │   Google OAuth 2.0       │
                    │   (Authentication)       │
                    └──────────────────────────┘
```

---

## Authentication Flow

```
┌─────────┐                                                    ┌─────────┐
│ Browser │                                                    │  Server │
└────┬────┘                                                    └────┬────┘
     │                                                              │
     │  1. Click "Continue with Google"                            │
     ├─────────────────────────────────────────────────────────────>
     │  GET /api/auth/google                                       │
     │                                                              │
     │  2. Redirect to Google OAuth                                │
     <─────────────────────────────────────────────────────────────┤
     │  302 https://accounts.google.com/...                        │
     │                                                              │
┌────┴────┐                                                         │
│ Google  │                                                         │
│  OAuth  │                                                         │
└────┬────┘                                                         │
     │  3. User signs in with Google                               │
     │                                                              │
     │  4. Google redirects back with code                         │
     ├─────────────────────────────────────────────────────────────>
     │  GET /api/auth/google/callback?code=...                     │
     │                                                              │
     │                                    5. Exchange code for user│
     │                                       Create/find user in DB│
     │                                       Generate JWT token    │
     │                                                              │
     │  6. Set JWT cookie & redirect to frontend                   │
     <─────────────────────────────────────────────────────────────┤
     │  302 http://localhost:5173?auth=success                     │
     │  Set-Cookie: token=jwt_token; HttpOnly                      │
     │                                                              │
     │  7. Frontend detects auth success                           │
     │  GET /api/auth/me (with cookie)                             │
     ├─────────────────────────────────────────────────────────────>
     │                                                              │
     │                                    8. Verify JWT from cookie│
     │                                       Return user data      │
     │                                                              │
     │  9. User data returned                                      │
     <─────────────────────────────────────────────────────────────┤
     │  { success: true, user: {...} }                             │
     │                                                              │
     │  10. Store user in Redux & redirect to dashboard            │
     │                                                              │
```

---

## Data Flow

### Creating a Project

```
┌─────────┐         ┌─────────┐         ┌─────────┐         ┌─────────┐
│ Browser │         │  Redux  │         │  Server │         │ MongoDB │
└────┬────┘         └────┬────┘         └────┬────┘         └────┬────┘
     │                   │                   │                   │
     │ 1. Fill form      │                   │                   │
     │ & submit          │                   │                   │
     │                   │                   │                   │
     │ 2. dispatch       │                   │                   │
     │ projectAPI.create │                   │                   │
     ├──────────────────>│                   │                   │
     │                   │                   │                   │
     │                   │ 3. POST /api/projects                 │
     │                   │ (with JWT cookie) │                   │
     │                   ├──────────────────>│                   │
     │                   │                   │                   │
     │                   │                   │ 4. Verify JWT     │
     │                   │                   │ & create project  │
     │                   │                   ├──────────────────>│
     │                   │                   │                   │
     │                   │                   │ 5. Project saved  │
     │                   │                   <──────────────────┤
     │                   │                   │                   │
     │                   │ 6. Return project │                   │
     │                   <──────────────────┤                   │
     │                   │                   │                   │
     │ 7. Update Redux   │                   │                   │
     │ state             │                   │                   │
     <──────────────────┤                   │                   │
     │                   │                   │                   │
     │ 8. UI updates     │                   │                   │
     │ automatically     │                   │                   │
     │                   │                   │                   │
```

---

## Deployment Architecture

### Development
```
┌──────────────────┐         ┌──────────────────┐
│   localhost:5173 │────────>│   localhost:5000 │
│   (Frontend)     │  API    │   (Backend)      │
└──────────────────┘         └──────────────────┘
                                      │
                                      ↓
                             ┌──────────────────┐
                             │  MongoDB Atlas   │
                             │  (Cloud)         │
                             └──────────────────┘
```

### Production
```
┌──────────────────┐         ┌──────────────────┐
│   Vercel/Netlify │────────>│  Render/Railway  │
│   (Frontend)     │  HTTPS  │   (Backend)      │
│   Static Files   │  API    │   Node.js        │
└──────────────────┘         └──────────────────┘
                                      │
                                      ↓
                             ┌──────────────────┐
                             │  MongoDB Atlas   │
                             │  (Cloud)         │
                             └──────────────────┘
```

---

## Security Layers

```
┌─────────────────────────────────────────────────────────────┐
│                    Security Measures                         │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  1. Authentication Layer                                     │
│     ├─ Google OAuth 2.0 (Trusted provider)                 │
│     ├─ JWT tokens (Stateless, scalable)                    │
│     └─ HTTP-only cookies (XSS protection)                  │
│                                                              │
│  2. Authorization Layer                                      │
│     ├─ Protected routes (Middleware)                        │
│     ├─ Token verification on every request                  │
│     └─ User context in requests                            │
│                                                              │
│  3. Network Layer                                            │
│     ├─ CORS configuration (Allowed origins)                │
│     ├─ HTTPS in production                                  │
│     └─ Secure cookies (secure flag)                        │
│                                                              │
│  4. Data Layer                                               │
│     ├─ MongoDB Atlas (Encrypted at rest)                   │
│     ├─ Network access control (IP whitelist)               │
│     └─ Database authentication                             │
│                                                              │
│  5. Application Layer                                        │
│     ├─ Environment variables (Secrets)                      │
│     ├─ Input validation                                     │
│     └─ Error handling (No sensitive data leak)             │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## Technology Stack Details

### Frontend Stack
```
React 19.1.1
├── Vite 7.1.2 (Build tool)
├── Redux Toolkit (State management)
│   ├── authSlice
│   ├── workspaceSlice
│   └── themeSlice
├── React Router DOM (Routing)
├── Axios (HTTP client)
├── Tailwind CSS (Styling)
├── Lucide React (Icons)
├── Recharts (Charts)
├── date-fns (Date handling)
└── react-hot-toast (Notifications)
```

### Backend Stack
```
Node.js + Express.js
├── Mongoose (MongoDB ODM)
├── Passport.js (Authentication)
│   └── passport-google-oauth20
├── jsonwebtoken (JWT)
├── express-session (Session management)
├── bcryptjs (Password hashing - future use)
├── cookie-parser (Cookie handling)
├── cors (CORS middleware)
└── dotenv (Environment variables)
```

### Database Schema
```
MongoDB Atlas
├── users
│   ├── _id (ObjectId)
│   ├── googleId (String, unique)
│   ├── name (String)
│   ├── email (String, unique)
│   └── image (String)
│
├── workspaces
│   ├── _id (ObjectId)
│   ├── name (String)
│   ├── slug (String, unique)
│   ├── ownerId (ObjectId → users)
│   └── members (Array)
│       ├── userId (ObjectId → users)
│       └── role (String: ADMIN/MEMBER)
│
├── projects
│   ├── _id (ObjectId)
│   ├── name (String)
│   ├── workspaceId (ObjectId → workspaces)
│   ├── team_lead (ObjectId → users)
│   ├── status (String)
│   ├── priority (String)
│   └── members (Array)
│
└── tasks
    ├── _id (ObjectId)
    ├── projectId (ObjectId → projects)
    ├── title (String)
    ├── assigneeId (ObjectId → users)
    ├── status (String)
    ├── type (String)
    ├── priority (String)
    └── due_date (Date)
```

---

## Scalability Considerations

### Current Architecture Benefits
- ✅ Stateless authentication (JWT) - Easy horizontal scaling
- ✅ Separate frontend/backend - Independent scaling
- ✅ MongoDB Atlas - Managed, auto-scaling database
- ✅ API-first design - Can add mobile apps easily

### Future Enhancements
- 🔄 Redis for session caching
- 🔄 WebSocket for real-time updates
- 🔄 CDN for static assets
- 🔄 Load balancer for multiple backend instances
- 🔄 Microservices architecture (if needed)

---

## Performance Optimization

### Frontend
- Code splitting (React.lazy)
- Image optimization
- Lazy loading
- Memoization (useMemo, useCallback)
- Virtual scrolling for large lists

### Backend
- Database indexing
- Query optimization
- Response caching
- Compression middleware
- Rate limiting

### Database
- Proper indexes on frequently queried fields
- Aggregation pipelines for complex queries
- Connection pooling
- Read replicas (if needed)
